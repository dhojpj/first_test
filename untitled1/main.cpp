#include <iostream>

using namespace std;

int main()
{
    // this edit was done on Qt Creator on PC
    cout << "Hello Qt Creator!" << endl;
    
    // this edit was done using Textastic with Working Copy as the repository
    cout << "Hello Textastic!" << endl; // for some reason Working Copy cannot add text to the files -- it's purely a repository (i.e. it just stores files)
    return 0;
}

